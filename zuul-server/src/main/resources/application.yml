eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:18080/eureka/
server:
  port: 18081
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: service-ribbon